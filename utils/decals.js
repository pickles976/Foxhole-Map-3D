import * as THREE from 'three'
import { DecalGeometry } from 

const raycaster = new THREE.raycaster()
const position =  new THREE.Vector3(MAP_SIZE / 2, 1024, MAP_SIZE / RATIO / 2)

const decalGeometry = new THREE.DecalGeometry()